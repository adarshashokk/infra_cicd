name: Terraform Plan

on:
  push:
    branches:
      - main


jobs:
  terraform:
    name: 'Terraform'
    runs-on: 'ubuntu-latest'
    environment:  "Dev"

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
